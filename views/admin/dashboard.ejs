<main>
	<%- include('../partials/adminSidebar') %>
<!--This includes the sidebar component for easy navigation across the admin panel.-->
	
	<div id="main-wrapper">
		
		<div class="bg-white shadow-sm p-3">
			<span class="me-3" id="sidebar-toggler-btn"><i class="fas fa-bars"></i></span>
			<h5 class="m-0 color-theme d-inline-block">Dashboard</h5>
		</div>
<!--The main wrapper contains the dashboard content, with a header that has a sidebar toggle button and the page title ("Dashboard").-->
		
		<div class="d-flex gap-3 flex-wrap m-4 text-white">
<!--A flexible container using Bootstrap's d-flex, gap-3, and flex-wrap classes for responsive, spaced elements.
Each card displays key statistics with dynamic values passed from the server-side (numAdmins, numDonors, etc.).-->
			
			<div class="bg-primary rounded p-3" style="width: 250px;">
				<div class="fs-1"><%= numAdmins %></div>
				<div class="fs-5">admins</div>
			</div>
			
			<div class="bg-success rounded p-3" style="width: 250px;">
				<div class="fs-1"><%= numDonors %></div>
				<div class="fs-5">donors</div>
			</div>
			
			<div class="bg-primary rounded p-3" style="width: 250px;">
				<div class="fs-1"><%= numAgents %></div>
				<div class="fs-5">agents</div>
			</div>
			
			<div class="bg-success rounded p-3" style="width: 250px;">
				<div class="fs-1"><%= numPendingDonations %></div>
				<div class="fs-5">new donation requests</div>
			</div>
			
			<div class="bg-warning rounded p-3" style="width: 250px;">
				<div class="fs-1"><%= numAcceptedDonations %></div>
				<div class="fs-5">donations to be assigned to agent</div>
			</div>
			
			<div class="bg-danger rounded p-3" style="width: 250px;">
				<div class="fs-1"><%= numAssignedDonations %></div>
				<div class="fs-5">donations not collected yet</div>
			</div>
			
			<div class="bg-success rounded p-3" style="width: 250px;">
				<div class="fs-1"><%= numCollectedDonations %></div>
				<div class="fs-5">donations collected</div>
			</div>
<!--bg-primary, bg-success, bg-warning, bg-danger: Bootstrap color classes for different sections (blue for admins/agents, green for donors, yellow for donations to be assigned, red for not collected donations).
rounded: Rounds the corners of each card.
p-3: Adds padding around the content.
fs-1, fs-5: Controls the font sizes for the numbers and labels, respectively.-->
			
		</div>
		
	</div>
</main>
